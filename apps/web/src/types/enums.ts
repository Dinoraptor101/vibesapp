// Enum definitions
export enum ThemeType {
  LIGHT = 'light',
  DIM = 'dim',
  DARK = 'dark',
}

export enum PostStatus {
  ACTIVE = 'active',
  HIDDEN = 'hidden',
  DELETED = 'deleted',
}

export enum VibesAction {
  CREATE_POST = 'CREATE_POST',
  RECEIVE_LIKE = 'RECEIVE_LIKE',
  RECEIVE_DISLIKE = 'RECEIVE_DISLIKE',
  SEND_DM = 'SEND_DM',
  RECEIVE_REPLY = 'RECEIVE_REPLY',
}

export enum SexType {
  MALE = 'Male',
  FEMALE = 'Female',
  OTHER = 'Other',
}

export enum PolarityType {
  YIN = 'yin',
  YANG = 'yang',
}
